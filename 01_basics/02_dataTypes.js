"use strict" // treat all js code as newer version

// alert( 3 + 3) // we are using node js not browser

console.log(3
    +
    3); // dont do that like line no 5 to 7 code redablity should be high

console.log("Sudheer")

let name = "hitesh"
let age = 18
let isLoggedIn = false

// //they are primitive data types
// number => 2 to power 53
// bigint => uses in big website where number are using ex- stocks reddit
// string  => string are uses in single or double quotes
// bollean => true/false
// null =>(null is object type) it is a stand alone value we can say its data type represation is empty value
// undefined => value is not assigned only declared
// symbol => unique

// //objects
console.log(typeof null) // result: object
console.log(typeof undefined) // result: undefined